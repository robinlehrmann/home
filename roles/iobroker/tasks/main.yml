---
- name: Re-create a iobroker container
  docker_container:
    name: iobroker
    image: buanet/iobroker
    state: started
    network_mode: host
    auto_remove: yes
    devices:
      - /dev/ttyACM0:/dev/ttyACM0
    volumes:
      - /iobroker
